#将静态内容部署到GITHUB页面的简单工作流nname:将静态内容部署到页面n non: n #在以默认分支为目标的推送上运行n push: n分支:[ "main "] n n #允许您从操作选项卡手动运行此工作流n workflow_dispatch: n n#设置GITHUB_TOKEN的权限以允许部署到GitHub页面n权限:n内容:n页面:write n id-token: write n n# 允许一个并发部署n并发:n组:" pages " n取消-进行中:true n作业:n #单个部署作业，因为我们正在部署n部署:n环境:n名称:github-Pages n URL:$ { { steps . deployment . outputs . page _ URL } } n运行-on:Ubuntu-最新n步骤:n名称:Checkout n用途:actions/checkout@v3 n名称:Setup Pages n用途:actions/configure-pages@v2 n名称:Upload artifact n用途:actions/upload-pages-artifact n-name:Deploy to GitHub Pages n id:deployment n uses:actions/Deploy-Pages @ v1
